[package]
name = "burn-dispatch-test-plugin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[features]
default = []
variant-b = []

[dependencies]
burn-dylib = { path = "../../../../burn-dylib", default-features = false }

[workspace]
